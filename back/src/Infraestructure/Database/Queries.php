<?php

namespace TodosList\Infraestructure\Database;

class Queries
{
    // public static $allHotelsWhereName = "SELECT 'hotel' type, name, stars, standar_room_type, city, state, country FROM hotels WHERE name like :search;";
    // public static $allApartmentsWhereName = "SELECT 'apartment' type, name, how_many, size, city, state, country FROM apartments WHERE name like :search;";
}
